# Family ABC Journal — Claude Code Context

Read docs/project-brief.md before starting any work.

## Quick Reference
- **App location:** `C:\Dev\family-journal`
- **Main file:** `src/App.jsx`
- **Run app:** `npm run dev` then open `http://localhost:5173`
- **Save to GitHub:** `git add .` → `git commit -m "message"` → `git push`
- **Repo:** https://github.com/n8-icanbury/family-journal

## Rules
- Never use localStorage or sessionStorage — use `window.storage?.` with optional chaining
- Keep Apple iOS HIG design throughout — system colors, SF Pro font, grouped sections
- Always commit after completing a feature
- Keep docs/project-brief.md updated after significant changes
